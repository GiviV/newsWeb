import { HoverPostDirective } from './hover-post.directive';

describe('HoverPostDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPostDirective();
    expect(directive).toBeTruthy();
  });
});
